<%= form_for(@food) do |f| %>
  <% if @food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% @food.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label>What kind of food is it?</label>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <label>Tell the world about your food:</label>
    <%= f.text_field :desc %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.select :category, ['veg', 'nonveg'] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
